# Capabilities
allow fsck_untrusted self:capability sys_admin;

# FUSE
allow fsck_untrusted fuse_device:chr_file rw_file_perms;

# Media file access
allow fsck_untrusted mnt_media_rw_file:dir r_dir_perms;
allow fsck_untrusted mnt_media_rw_stub_file:dir { r_dir_perms mounton };

# Mount
allow fsck_untrusted unlabeled:filesystem mount;
#allow fsck_untrusted mount_exec:file entrypoint;
